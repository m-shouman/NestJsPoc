import { EmployeeDataValidationPipe } from './employee-data-validation.pipe';

describe('EmployeeDataValidationPipe', () => {
  it('should be defined', () => {
    expect(new EmployeeDataValidationPipe()).toBeDefined();
  });
});
